<div class='header'>
  <div class='header-left'>
    <h1 class="glitch">
      <span aria-hidden="true">A SONIC AFFAIR</span>
        A SONIC AFFAIR
      <span aria-hidden="true">A SONIC AFFAIR</span>
    </h1>
    <section class="subtitle">
      <div class="top">ASSASSINATION OF THE WHITE TIGER</div>
      <div class="bottom" aria-hidden="true">ASSASSINATION OF THE WHITE TIGER</div>
    </section>
  </div>

  <div class='nav'>
    <a href="/" class='nav-item'>
      HOME
    </a>
    <a href="/about/" class='nav-item'>
      FAQ
    </a>
    <a href="/tickets/" class='nav-item'>
      TICKETS
    </a>
  </div>
</div>

<style lang="scss">
  .nav {
    margin-top: 1rem;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;

    @media (max-width: 1200px) {
      width: 100%;
      flex-direction: row;
      justify-content: space-between;
    }
  }

  .nav-item {
    mix-blend-mode: difference;
    padding: 0.5rem;
    background-color: white;
    color: black;
    font-size: var(--text-size);
    font-family: 'Rubik';
    font-weight: 500;
    text-align: center;
    border-radius: 0.75rem;
    text-decoration: none;

    &:hover {
      background-color: black;
      color: white;
      mix-blend-mode: normal;
    }

    @media (max-width: 1200px) {
      flex-grow: 1;
      font-size: var(--mobile-text-size-large);
    }
  }

  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;

    @media (max-width: 1200px) {
      flex-direction: column;
      gap: 0;
    }
  }

  .glitch {
    display: flex;
    text-align: center;
    color: white;
    font-family: 'Rubik', sans-serif;
    font-weight: 500;
    margin: 0;
    font-size: 7.5rem;
    font-weight: bold;
    text-transform: uppercase;
    position: relative;
    text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
      0.025em 0.04em 0 #fffc00;
    animation: glitch 100s infinite;

    @media (max-width: 1200px) {
      font-size: 2.5rem;
      justify-content: center;
    }
  }

  .glitch span {
    position: absolute;
  }

  .glitch span:first-child {
    animation: glitch 1s infinite;
    clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
    transform: translate(-0.04em, -0.03em);
    opacity: 0.75;
  }

  .glitch span:last-child {
    animation: glitch 2s infinite;
    clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
    transform: translate(0.04em, 0.03em);
    opacity: 0.75;
  }

  @keyframes glitch {
    0% {
      text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
        0.025em 0.04em 0 #fffc00;
    }
    15% {
      text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
        0.025em 0.04em 0 #fffc00;
    }
    16% {
      text-shadow: -0.05em -0.025em 0 #00fffc, 0.025em 0.035em 0 #fc00ff,
        -0.05em -0.05em 0 #fffc00;
    }
    49% {
      text-shadow: -0.05em -0.025em 0 #00fffc, 0.025em 0.035em 0 #fc00ff,
        -0.05em -0.05em 0 #fffc00;
    }
    50% {
      text-shadow: 0.05em 0.035em 0 #00fffc, 0.03em 0 0 #fc00ff,
        0 -0.04em 0 #fffc00;
    }
    99% {
      text-shadow: 0.05em 0.035em 0 #00fffc, 0.03em 0 0 #fc00ff,
        0 -0.04em 0 #fffc00;
    }
    100% {
      text-shadow: -0.05em 0 0 #00fffc, -0.025em -0.04em 0 #fc00ff,
        -0.04em -0.025em 0 #fffc00;
    }
  }

  .subtitle {
    display: grid;
    font-family: "Rubik", sans-serif;
    font-size: 3rem;
    font-weight: 500;
    text-transform: uppercase;
    color: white;

    @media (max-width: 1200px) {
      font-size: var(--mobile-text-size-large);
      text-align: center;
      justify-content: center;
    }
  }

  .subtitle > div {
    grid-area: 1/1/-1/-1;
  }
  .top {
    clip-path: polygon(0% 0%, 100% 0%, 100% 48%, 0% 58%);
  }
  .bottom {
    clip-path: polygon(0% 60%, 100% 50%, 100% 100%, 0% 100%);
    color: '#cbcbcb';
    background-clip: text;
    -webkit-background-clip: text;
    transform: translateX(-0.04em);
  }
</style>
